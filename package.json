{
  "name": "gonzalo-espinosa-hacienda",
  "version": "0.1.0",
  "private": true,
  "description": "Sistema web para operaciones, liquidaciones y cuenta corriente de consignataria de hacienda.",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "check": "node --check server.js && node --check data-source.js && node --check public/app.js"
  },
  "optionalDependencies": {
    "pg": "^8.21.0"
  }
}
